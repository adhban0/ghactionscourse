on:
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - working-directory: /tmp
        shell: bash
        run: echo "$0"
      - run: |
          import sys
          print(sys.version)
        shell: python
